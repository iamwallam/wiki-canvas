        /** @type {import('next').NextConfig} */
        const nextConfig = {
            env: {
              OPENROUTER_KEY: process.env.OPENROUTER_KEY,
            },
          };
          export default nextConfig;